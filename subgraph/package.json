{
    "name": "multisig-delegate-wallet",
    "license": "UNLICENSED",
    "scripts": {
        "prepare": "graph codegen && graph build --network mumbai --network-file networks.json",
        "codegen": "graph codegen",
        "build": "graph build",
        "auth": "graph auth --product hosted-service",
        "deploy": "graph deploy --product hosted-service nonitmittal/multisig-delegate-wallet"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.33.0",
        "@graphprotocol/graph-ts": "0.27.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
